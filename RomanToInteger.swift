import UIKit


func romanToInt(_ roman: String) -> Int {
    //for character in roman {
        //for value in key {
            //if character == X or M,C,B all characters for cases
    // check for the one after
    // skip iteration of the next
    // if its possible to equal the value ( not nil ) do it and add previous value += to sum
    //
       // }
   // }
    return 0
}


